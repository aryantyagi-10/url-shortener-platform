# This file marks the 'app' directory as a Python package.
# Without this file, Python won't recognize 'app' as a module
# and imports like "from app.database import ..." will fail.
# It can be empty — its mere existence is what matters.
